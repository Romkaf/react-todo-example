import React from "react";

const SearchPanel = () => {
  const searchText = "Type here for search";
  return <input style={{ fontSize: "20px" }} placeholder={searchText}></input>;
};

export default SearchPanel;
